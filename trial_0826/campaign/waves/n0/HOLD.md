# SUPERSEDED (2026-08-27) — never submit this wave

The free-B d=12 design in this directory is replaced by the derived-bid
ω-only scheme (math-log §1–2): B is no longer a free variable
(B = 20·ρ_H2 per scenario), so every (ω, B) Sobol point here samples a
dimension that no longer exists. **Never submit this wave.** It is kept as
the d12-era archive (audit trail) — do not delete, do not regenerate.

If BO proceeds, a v3 initial design is n₀ = 64 per ρ scenario at d = 6.
